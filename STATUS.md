# Release status

Current status: **Candidate** — the `E2E` tutorial notebook `tutorials/countgd_object_counting_colab.ipynb` executed top-to-bottom in a fresh CPU kernel on the build workstation (a pre-flight, recorded in `docs/release-verification.md`); no clean hosted-runtime execution of the committed notebook blob is recorded yet. At this revision the pipeline package with its counting, evaluation and adaptation contracts, the pickle audit and deterministic conversion of the pinned checkpoint, the offline unit suite, the model-backed regressions, `MODEL_CARD.md` (MODEL_CARD_SPEC 1.2), the static validator (`tools/validate_release_assets.py`), the generator parity checks and the CI workflow exist and pass locally. The status becomes **Release-grade** when a clean hosted runtime executes the exact committed notebook blob; any later change to the carried modules or to the notebook yields a new blob that needs its own clean run.
