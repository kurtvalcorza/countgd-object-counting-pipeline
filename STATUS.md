# Release status

Current status: **Release-grade** — the `E2E` tutorial notebook `tutorials/countgd_object_counting_colab.ipynb` (blob `c619a762`, committed at `8d61b94`) executed top-to-bottom in a clean Kaggle Tesla T4 runtime on 2026-09-24 (16/16 code cells ok, one interpreter restart after the install cell, 468.7 s), recorded in `docs/release-verification.md`; the same blob also ran in a fresh CPU kernel on the build workstation as a pre-flight. At that revision the pipeline package with its counting, evaluation and adaptation contracts, the pickle audit and deterministic conversion of the pinned checkpoint, the offline unit suite, the model-backed regressions, `MODEL_CARD.md` (MODEL_CARD_SPEC 1.2), the static validator (`tools/validate_release_assets.py`), the generator parity checks and the CI workflow exist and pass locally. Any later change to the carried modules or to the notebook yields a new blob and returns the status to **Candidate** until a clean run of that blob is recorded.
